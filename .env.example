# API Keys — comma-separated list of valid keys
API_KEYS=dev-key-healthrag,prod-key-change-me

# Embedding model (multilingual, handles EN + JA)
EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2

# Translation backend:
#   "googletrans"    — py-googletrans (needs manual install — see requirements.txt note)
#   "argostranslate" — Argos Translate offline neural MT (~100 MB per direction)
#   "google"         — Google Translate via deep-translator (needs internet, no API key)
#   "mock"           — placeholder tags only (for tests and CI, no download needed)
TRANSLATION_BACKEND=google

# FAISS index storage path
FAISS_INDEX_PATH=data/faiss_index

# Chunking
CHUNK_SIZE=512
CHUNK_OVERLAP=50

# Generation context window (characters)
MAX_CONTEXT_CHARS=2000

# LLM backend for /generate:
#   "template" — EN/JA prompt templates, no API key (default)
#   "openai"   — OpenAI Chat API (pip install langchain-openai, set OPENAI_API_KEY)
#   "claude"   — Anthropic API (pip install langchain-anthropic, set ANTHROPIC_API_KEY)
LLM_BACKEND=template

# Model override — leave unset to use each backend's default
# LLM_MODEL=gpt-4o-mini

# API keys for real LLM backends
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# Logging
LOG_LEVEL=INFO
ENVIRONMENT=development

# Log file output (optional — leave empty or unset for stdout only)
# LOG_FILE=logs/healthrag.log
# LOG_MAX_BYTES=10485760
# LOG_BACKUP_COUNT=5
